services:
  proxylens-server:
    build:
      context: ../..
      dockerfile: examples/chained-apps-no-propagation/server.Dockerfile
    command:
      - proxylens-server
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --data-dir
      - /data
      - --no-access-log
    environment:
      PYTHONUNBUFFERED: "1"
    ports:
      - "8000:8000"
    volumes:
      - ./.data/server:/data
    networks:
      - chained-apps-no-propagation

networks:
  chained-apps-no-propagation:
    name: proxylens-chained-apps-no-propagation
    external: true
